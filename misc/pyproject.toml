[project]
name = "misc"
version = "0.1.0"
description = "Misc utils code"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "accelerate>=1.12.0",
    "huggingface-hub>=0.35.1",
    "torch>=2.9.0",
    "transformers>=4.57.1",
]

[dependency-groups]
dev = [
    "memory-profiler>=0.61.0",
]
