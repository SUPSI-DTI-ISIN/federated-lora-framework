[project]
name = "federated-learning-service"
version = "0.1.0"
description = "Federated Learning service which contains client app and server app to orchestrate FL fine tuning"
requires-python = ">=3.12.12"
dependencies = [
    "bitsandbytes>=0.49.1",
    "datasets>=4.4.2",
    #"federated-learning-client",
    #"federated-learning-server",
    "flwr[simulation]>=1.25.0",
    "peft>=0.18.0",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
    "pymupdf>=1.26.7",
    "torch>=2.9.1",
]

[tool.flwr.app]
publisher = "luca.fanto"

[tool.flwr.app.components]
serverapp = "src.server_app:app"
clientapp = "src.client_app:app"


[tool.flwr.app.config]
num-server-rounds = 3
fraction-train = 0.5
local-epochs = 1
lr = 0.01

[tool.flwr.federations]
default = "local-simulation"

[tool.flwr.federations.local-simulation]
options.num-supernodes = 1

[tool.flwr.federations.local-deployment]
address = "127.0.0.1:9093"
insecure = true

[tool.flwr.federations.nvidia-deployment]
address = "10.11.13.6:9093"
insecure = true

# Remote federation example for use with SuperLink
[tool.flwr.federations.remote-federation]
address = "<SUPERLINK-ADDRESS>:<PORT>"
insecure = true  # Remove this line to enable TLS

# root-certificates = "<PATH/TO/ca.crt>"  # For TLS setup

#[tool.uv.sources]
#federated-learning-server = { path = "../department/federated-learning-server" }
#federated-learning-client = { path = "../institute/federated-learning-client" }

#[[tool.uv.index]]
#name = "gitlab"
#url = "https://gitlab-edu.supsi.ch/api/v4/projects/1401/packages/pypi/simple"
#authenticate = "always"